---
description: External integration encapsulation (ts-morph, git, commander, schemas)
globs: src/git/**,src/complexity/**,src/scoring/**,src/report/**,src/compare/**,src/config/**,bin/**,package.json,schemas/**
alwaysApply: false
---

# External Integrations

Follow [INTEGRATIONS.md](../../.specs/codebase/INTEGRATIONS.md):

- **ts-morph** — sole AST access point; mock at the `ComplexityAnalyzer` boundary, not in scorers or reporter.
- **git** — `child_process.spawn` encapsulated in `GitMiner`; do not spawn git outside `src/git/`.
- **commander** — CLI parsing in `bin/hotspot-scanner.ts`; no domain logic in the bin.
- **JSON schemas** — published contracts in `schemas/`; `loadBaseline` validates against the domain contract (do not scatter ad hoc parsers).

## Rules

- Encapsulate adapters (`GitMiner`, `ComplexityAnalyzer`) — do not spread ts-morph/git imports across the pipeline.
- `src/report/` stays pure (no `fs`); file I/O in bin / compare loader.
- `src/config/` — only `.hotspot-scanner.json`; precedence CLI > config > defaults.
- Tests mock at the adapter boundary (see TESTING.md § Mock boundaries).
- New runtime dependencies require an `INTEGRATIONS.md` entry and justification in the feature design.

## Observability

Integration errors must propagate with context (repo/file path, git command, scan phase) — see patterns in `INTEGRATIONS.md`.
