---
description: Keep STRUCTURE.md as present-tense directory-layout / public-API SoT — no milestone changelog
globs: .specs/codebase/STRUCTURE.md
alwaysApply: false
---

# Structure Layout SoT

**Role:** directory layout + public API map. **Not:** flag encyclopedia, testing guide, or pipeline design.

Ownership: [DOC-OWNERSHIP.md](../../.specs/codebase/DOC-OWNERSHIP.md). Lint: `lintStructureDoc`.

## Forbidden

- `\bM\d+\b`, `HOTSPOT-*`, changelog voice; CLI flags → cli-reference; fixtures → TESTING; pipelines → ARCHITECTURE

## Allowed

- Trees, Path|Role tables, exports aligned with `src/index.ts`; pointers to ARCHITECTURE / TESTING / STACK

## Sync

Update only for live layout / public API. Milestone-needed prose → ROADMAP/STATE/feature specs.
