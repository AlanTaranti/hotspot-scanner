# Code Analysis

Prefer workspace tools already available to the agent (Grep/Glob/Read, or `rg` in Shell). Use `ast-grep` (`sg`) when structural patterns help and the binary is installed.

No hotspot-scanner-specific analysis pipeline — stay evidence-based against `.specs/codebase/` and the target `src/` modules.
