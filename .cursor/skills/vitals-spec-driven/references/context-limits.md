# Context Limits

Soft guidance only — not enforced by hooks except line-count warns on some living docs (see `living-sot-doc.mjs`).

Prefer progressive disclosure: load AGENTS + vitals-project + the active feature artifacts + the current phase ref. Do not preload the full `references/` tree.

Living-doc size discipline: follow each `*-sot.mdc` / soft warn thresholds in hooks.
