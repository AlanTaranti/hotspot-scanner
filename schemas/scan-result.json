{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/AlanTaranti/hotspot-scanner/main/schemas/scan-result.json",
  "title": "ScanResult",
  "description": "Full scan output from hotspot-scanner (JSON format).",
  "type": "object",
  "additionalProperties": true,
  "required": ["version", "hotspots", "meta"],
  "properties": {
    "$schema": { "type": "string" },
    "version": {
      "type": "string",
      "const": "3.0"
    },
    "hotspots": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/HotspotScore"
      }
    },
    "meta": {
      "$ref": "#/$defs/ScanMeta"
    }
  },
  "$defs": {
    "HotspotScore": {
      "type": "object",
      "additionalProperties": true,
      "required": [
        "filePath",
        "complexityNormalized",
        "churnNormalized",
        "hotspotScore",
        "ncloc",
        "commitCount",
        "linesChanged",
        "authorCount"
      ],
      "properties": {
        "filePath": { "type": "string" },
        "complexityNormalized": { "type": "number" },
        "churnNormalized": { "type": "number" },
        "hotspotScore": { "type": "number" },
        "ncloc": { "type": "number" },
        "commitCount": { "type": "integer" },
        "linesChanged": { "type": "integer" },
        "authorCount": { "type": "integer" }
      }
    },
    "DiagnosticSeverity": {
      "type": "string",
      "enum": ["info", "warning", "error"]
    },
    "ScanWarning": {
      "type": "object",
      "additionalProperties": true,
      "required": ["severity", "message"],
      "properties": {
        "severity": { "$ref": "#/$defs/DiagnosticSeverity" },
        "message": { "type": "string" },
        "code": { "type": "string" }
      }
    },
    "ScanStageTimings": {
      "type": "object",
      "additionalProperties": true,
      "required": ["gitMs", "complexityMs", "totalMs"],
      "properties": {
        "gitMs": { "type": "integer", "minimum": 0 },
        "complexityMs": { "type": "integer", "minimum": 0 },
        "totalMs": { "type": "integer", "minimum": 0 }
      }
    },
    "ScanMeta": {
      "type": "object",
      "additionalProperties": true,
      "required": ["since", "scannedAt", "warnings"],
      "properties": {
        "since": { "type": "string" },
        "scannedAt": { "type": "string" },
        "warnings": {
          "type": "array",
          "items": { "$ref": "#/$defs/ScanWarning" }
        },
        "timings": { "$ref": "#/$defs/ScanStageTimings" },
        "scannerVersion": { "type": "string" }
      }
    }
  }
}
